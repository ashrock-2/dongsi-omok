[phases.setup]
  nodejs = "22.1.0"
  pnpm = "9.6.0"

[phases.install]
  command = "pnpm install --no-frozen-lockfile"

[phases.build]
  command = "pnpm run build"

[phases.start]
  command = "pnpm run start"
